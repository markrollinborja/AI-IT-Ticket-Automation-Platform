# Dependencies needed for local development and running the test suite.
# Install with: pip install -r api/requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-cov==7.1.0

# Starlette's TestClient prefers httpx2 over plain httpx (httpx is going
# stale upstream); without this it still works, just warns on every test
# run. This is test-only - the app itself doesn't use httpx at all.
httpx2==2.7.0

ruff==0.15.22
pre-commit==4.6.0
